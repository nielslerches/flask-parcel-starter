let container = document.getElementById('root');
let element = document.createElement('h1');
let text = document.createTextNode('Flask parcel starter');

element.appendChild(text);
container.appendChild(element);
